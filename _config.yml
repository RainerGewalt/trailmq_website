title: "TrailMQ"
description: "Open Source Messaging for Regulated Industries"
url: "https://trailmq.com"
baseurl: ""                 # leer lassen, wenn Website direkt unter der Root-Domain läuft

collections:
  pages:
    output: true

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default

plugins:
  - jekyll-sitemap
